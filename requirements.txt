# Core
playwright>=1.40.0
python-dotenv>=1.0.0
pydantic>=2.0
pydantic-settings>=2.0

# STT
deepgram-sdk>=3.0.0

# LLM – openai client works for OpenAI *and* any OpenAI-compatible local server (Ollama, LM Studio, etc.)
openai>=1.0.0
# anthropic>=0.18.0          # only if you use Claude
# google-generativeai        # only if you use Gemini

# TTS
elevenlabs>=1.0.0

# Audio helpers (platform dependent – install as needed)
# sounddevice
# pyaudio
# numpy

# Async
aiohttp
